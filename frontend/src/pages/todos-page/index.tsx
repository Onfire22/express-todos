import { TodosPageView } from './components/todos-page-view';
import { Todos } from './components/todos';

const TodosPage = () => {
	return (
		<TodosPageView>
			<Todos />
		</TodosPageView>
	);
};

export { TodosPage };
